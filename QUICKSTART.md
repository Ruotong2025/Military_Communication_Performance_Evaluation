## 手动启动

```bash
# 1. 编译项目
mvn clean install

# 2. 运行项目
mvn spring-boot:run
```

```bash
# 进入前端目录
cd frontend

# 安装依赖（首次运行）
npm install

# 启动前端
npm run dev
```

## 访问地址

- **API接口**: http://localhost:8080/api
- **API文档**: http://localhost:8080/api/swagger-ui.html
