# 动态定性评估集结模块改造方案

## 需求确认

### 用户需求
1. 集结范围只需要一个选项（不再区分全部/按层级/按作战）
2. 每次集结是 **10名专家**
3. 按 **每个作战ID、每个指标** 单独进行集结计算
4. 结果表格最左侧显示 **作战ID**，右侧显示集结后的结果
5. 按钮只保留一个 "执行集结"

### 数据流
```
专家评分 → 按作战ID分组 → 每个作战ID的每个指标单独集结 → 结果表格(左侧作战ID)
```

## 修改内容

### 1. 前端修改 (`DynamicQualitativeView.vue`)

#### 1.1 简化集结对话框
- 移除 "集结范围" 的3个选项（全部/按层级/按作战）
- 只保留权威度/把握度权重配置
- 按钮改为 "执行集结"

**修改前：**
```vue
<el-form-item label="集结范围">
  <el-radio-group v-model="aggregateMode">
    <el-radio label="all">全部集结</el-radio>
    <el-radio label="level">按当前层级</el-radio>
    <el-radio label="operation">按当前作战</el-radio>
  </el-radio-group>
</el-form-item>
```

**修改后：** 删除集结范围选项

#### 1.2 修改集结结果表格
- 左侧添加 "作战ID" 列
- 右侧显示各指标的集结结果

**新表格结构：**
| 作战ID | 指标名称 | 质心分 x* | 映射等级 | 等级区间 | 参与专家 | 操作 |
|--------|----------|------------|----------|----------|----------|------|
| OP001  | 通信抗毁性 | 82.5 | 良+ | [80,85) | 10 | 明细/质心图 |
| OP002  | 通信抗毁性 | 78.3 | 良 | [75,80) | 10 | 明细/质心图 |
| OP001  | 信息安全 | 85.0 | 优- | [85,90) | 10 | 明细/质心图 |
| ... | ... | ... | ... | ... | ... | ... |

### 2. 后端修改 (`DynamicQlService.java`)

#### 2.1 修改 `aggregateScores` 方法
- 不再接收 `operationId` 和 `levelName` 参数
- 自动遍历所有作战、所有层级、所有指标进行集结
- 为每个作战ID的每个指标单独计算并保存

**修改前逻辑：**
```java
// operationId 为 null 时，所有作战一起集结
if (operationId != null && levelName != null) {
    records = qlRecordRepository.findByBatchIdAndOperationId(...);
} else {
    records = qlRecordRepository.findByBatchId(...); // 全部
}
```

**修改后逻辑：**
```java
// 1. 获取所有作战ID
List<String> allOperations = getOperationsByBatch(batchId);

// 2. 获取所有层级
List<String> allLevels = getLevelNames(templateId);

// 3. 对每个作战ID的每个指标单独进行集结
for (String opId : allOperations) {
    for (String level : allLevels) {
        List<DynamicQlRecord> records = qlRecordRepository.findByBatchIdAndOperationIdAndLevelName(batchId, opId, level);
        // 按指标分组计算
        // 保存结果（使用 opId 区分）
    }
}
```

#### 2.2 修改返回结果结构
```json
{
  "success": true,
  "batchId": "BATCH001",
  "resultsByOperation": {
    "OP001": [
      { "indicatorCode": "QL001", "indicatorName": "通信抗毁性", "xStar": 82.5, "mappedGrade": "良+", ... },
      { "indicatorCode": "QL002", "indicatorName": "信息安全", "xStar": 85.0, "mappedGrade": "优-", ... }
    ],
    "OP002": [
      { "indicatorCode": "QL001", "indicatorName": "通信抗毁性", "xStar": 78.3, "mappedGrade": "良", ... }
    ]
  },
  "levelInfos": [...],
  "weights": {...}
}
```

### 3. 数据库存储

`dynamic_ql_aggregation` 表已有 `operation_id` 字段：
- 修改查询逻辑，确保按 `operation_id` 正确存储和读取
- 每个作战ID的每个指标都有一条独立的集结记录

## 实现步骤

### 步骤1：后端修改
1. 修改 `aggregateScores` 方法，遍历所有作战和指标进行集结
2. 修改返回结构，增加 `resultsByOperation`
3. 确保集结结果按作战ID分开存储

### 步骤2：前端修改
1. 简化集结对话框，移除集结范围选项
2. 修改 `handleAggregate` 函数，调用新的后端接口
3. 修改集结结果展示表格，添加作战ID列
4. 修改明细和质心图展示，支持按作战ID筛选

### 步骤3：测试验证
1. 选择批次和模板
2. 执行批量评估（10名专家）
3. 点击"执行集结"
4. 验证结果表格是否按作战ID分开展示

## 关键文件

| 文件 | 修改内容 |
|------|----------|
| `DynamicQlService.java` | 修改集结逻辑，按作战ID单独集结 |
| `DynamicQualitativeView.vue` | 简化对话框，修改结果表格 |

## 预期效果

1. **集结对话框**：只有权威度/把握度权重配置，一个"执行集结"按钮
2. **结果表格**：
   - 左侧：作战ID（如 OP001, OP002, OP003...）
   - 右侧：该作战各指标的集结结果（质心分、映射等级等）
   - 支持明细和质心图查看
3. **数据准确性**：每个作战ID的集结是独立计算的，10名专家的评分正确聚合
